%option noyywrap
%{
#include <iostream>
#include "RPcustomLOFTconfigparser.hxx"
int lineno=0;
#define YY_DECL extern "C" int MUConfiglex()
%}
%%
[\t] ;
start {return START;}
loft_config {return LOFT;}
MUTracker_config {return MUTracker;}
end {return END;}
\#.*|\/\/.* {return COMMENT; }
\n              {++lineno;}
-[0-9]+|-[0-9]+\.[0-9]+|[0-9]+\.[0-9]+|[0-9]+|[-+]?[0-9]*\.?[0-9]+([eE][-+]?[0-9]+) { MUConfiglval.fval = atof(yytext);return MY_FLOAT; }
[a-zA-Z0-9]+|_ {MUConfiglval.sval = strdup(yytext); return MY_STRING; }
\. {return DOT;}
\= {return EQUAL;}
([\/]+[\w]*).* {MUConfiglval.sval=strdup(yytext); return MY_FILEPATH;}
([a-zA-Z]+\:).* {MUConfiglval.sval=strdup(yytext);  return MY_FILEPATH;}
\; {return MY_SEMIC;}
.    {;}
%%


